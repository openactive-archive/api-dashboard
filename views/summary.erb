<div class="container">
<% if samples == 0 %>
  <p>A summary has not been generated for this dataset yet.</p>
<% else %>
  <div class="row">
  <div class="col-sm">
    <p><b>Top activities</b></p>
    <% if activities.size == 0 %>
      <p>No activities found for dataset.</p>
    <% else %> 
      <table class="table">
        <thead>
          <tr>
            <th>#</th>
            <th>Activity</th>
            <th>Opportunities</th>
          </tr>
        </thead>
        <tbody>
          <% activities.keys.each do |activity|; score = activities[activity] %>
          <tr>
            <th scope="row"><%= activities.keys.index(activity)+1 %></th>
            <td><%= activity %></td>
            <td><%= score.to_i %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    <% end %>
  </div>

  <div class="col-sm">
    <p><b>Top areas</b></p>
    <% if boundaries.size == 0 %>
      <p>No geogrpahic areas found for dataset.</p>
    <% else %>
      <table class="table">
        <thead>
          <tr>
            <th>#</th>
            <th>Authority Area</th>
            <th>Opportunities</th>
          </tr>
        </thead>
        <tbody>
          <% boundaries.keys.each do |boundary|; score = boundaries[boundary] %>
          <tr>
            <th scope="row"><%= boundaries.keys.index(boundary)+1 %></th>
            <td><%= boundary %></td>
            <td><%= score.to_i %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    <% end %>
  </div>
  </div>

  <p>Summary generated by sampling <%= samples %> opportunities since 2017.</p>

  <p>Please note opportunity counts are not exact and should only be used as a relative indication of dataset contents. Information on how the summary was generated can be found on the <a href="/about">about page</a>.</p>

  <p>Contains <a href="http://geoportal.statistics.gov.uk/datasets/local-authority-districts-december-2016-generalised-clipped-boundaries-in-the-uk/" target="_blank">National Statistics data</a> © Crown copyright and database right 2017</p>
<% end %>
</div>